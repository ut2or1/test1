$bin?=@build/k64-blinky.elf

echo "========================================="
echo "  K64 UART Test"
echo "========================================="

echo "Loading ELF file: $bin"

using sysbus

echo "Creating machine..."
mach create "k64-uart-test"

echo "Loading platform..."
machine LoadPlatformDescription @platforms/cpus/nxp-k6xf.repl

echo ""
echo "Setting up UART0 analyzer..."
showAnalyzer sysbus.uart0

echo ""
echo "Loading firmware..."
sysbus LoadELF $bin

echo ""
echo "Starting emulation..."
start

sleep 2.0

echo ""
echo "--- Test Results ---"

sysbus.cpu PC
sysbus.cpu PC $pc

if $pc != 0x1A1 {
    echo "PASS: CPU executing (PC = $pc)"
    echo ""
    echo "=== Test PASSED ==="
} else {
    echo "FAIL: CPU stuck at reset"
    echo ""
    echo "=== Test FAILED ==="
    quit 1
}
